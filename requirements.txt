# GEAR framework — acquisition/processing layer.
# Restricted to what this layer actually imports (see docs/INVENTORY.md,
# "Dependências"). Libraries used only by the not-yet-rebuilt index/weighting
# layer are added when that layer is written.

numpy>=1.24.0
pandas>=2.0.0
xarray>=2023.0.0
rioxarray>=0.14.0
rasterio>=1.3.0
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.5.0
earthengine-api>=0.1.350
cdsapi>=0.7.0
netcdf4>=1.6.0
h5netcdf>=1.2.0
requests>=2.31.0
openpyxl>=3.1.0
tqdm>=4.65.0
python-dotenv>=1.0.0

# Visualization (src/visualization/) -- CCRS figures for the article.
matplotlib>=3.7.0

# EM-DAT x Hazard spatial overlay validation (src/index/emdat_validation.py,
# C6, exploratory) -- Mann-Whitney U test.
scipy>=1.11.0

# Tests
pytest>=7.4.0
