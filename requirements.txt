# GEAR framework dependencies.
# Restricted to what the codebase actually imports (see docs/INVENTORY.md,
# "Dependências"). Grouped by the layer that introduced each dependency.

numpy>=1.24.0
# Ceiling at <3: the codebase was built and tested entirely against the
# pandas 2.x series. pandas 3.0 changes None->NaN coercion in object columns,
# which breaks existing None/NaN guards (risk_bands.worst_case_band,
# visualization/emdat_validation). Revisit when the code is migrated.
pandas>=2.0.0,<3.0.0
xarray>=2023.0.0
rioxarray>=0.14.0
rasterio>=1.3.0
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.5.0
earthengine-api>=0.1.350
cdsapi>=0.7.0
netcdf4>=1.6.0
h5netcdf>=1.2.0

# GRIB reader for era5_wind_downloader.py -- CDS's reanalysis-era5-single-levels
# response for instantaneous_10m_wind_gust is raw GRIB in practice, not a zip
# of NetCDF files; xarray's cfgrib engine opens it directly.
cfgrib>=0.9.15
eccodes>=2.48.0
requests>=2.31.0
openpyxl>=3.1.0
tqdm>=4.65.0
python-dotenv>=1.0.0

# Visualization (src/visualization/) -- CCRS figures for the article.
matplotlib>=3.7.0

# EM-DAT x Hazard spatial overlay validation (src/index/emdat_validation.py,
# C6, exploratory) -- Mann-Whitney U test.
scipy>=1.11.0

# GEAR v3 Phase 6 perf (src/index/psae.py, classify_psae_fraction_batch) --
# JIT-compiled per-row PSAE classification, the one pure-Python hotspot a
# cProfile of the Sobol/Monte-Carlo draw loop surfaced (~11% of per-draw
# time). See docs/DECISIONS.md, "GEAR v3 Phase 6 perf: Hazard_i,h cache +
# Numba classify_psae_fraction".
numba>=0.60.0

# Tests
pytest>=7.4.0
